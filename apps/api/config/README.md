# apps/api/config

Configuration files for the ForgePass API.

| File | Description |
|---|---|
| `indexer-config.json` | Indexer polling strategy for all v1 signal sources. Output of issue #004. See `../docs/indexer-strategy.md` for full decision rationale. |

